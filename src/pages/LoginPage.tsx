import { FC } from 'react';
import LoginForm from '../components/LoginForm/LoginForm';

const LoginPage: FC = () => {
  return (
    <div>
      <h1>Login</h1>
      <LoginForm />
    </div>
  );
};

export default LoginPage;
