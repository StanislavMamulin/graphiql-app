import { FC } from 'react';
import LoginForm from '../components/LoginForm/LoginForm';

const LoginPage: FC = () => {
  return (
    <div>
      <LoginForm />
    </div>
  );
};

export default LoginPage;
